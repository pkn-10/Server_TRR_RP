# ระบบแจ้งเตือนอักษร (Notification System)

## 📋 ภาพรวม
ระบบนี้ออกแบบให้แยกการแจ้งเตือนตามบัญชีผู้ใช้และบทบาท (Role) เพื่อให้ผู้ใช้แต่ละคนได้รับข้อมูลที่เกี่ยวข้องกับพวกเขาเท่านั้น

## 🏗️ สถาปัตยกรรม

### Database Schema
```prisma
model Notification {
  id        Int      @id @default(autoincrement())
  userId    Int      // ผู้ใช้ที่ได้รับแจ้งเตือน
  type      NotificationType
  status    NotificationStatus @default(UNREAD)
  title     String   // หัวข้อ
  message   String   // เนื้อความ
  ticketId  Int?     // เลขอ้างอิงงาน (ถ้ามี)
  actionUrl String?  // URL สำหรับกระทำการ
  
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum NotificationType {
  TICKET_CREATED
  TICKET_ASSIGNED
  TICKET_UPDATED
  TICKET_COMPLETED
  TICKET_REJECTED
  COMMENT_ADDED
  STATUS_CHANGED
}

enum NotificationStatus {
  UNREAD
  READ
}
```

## 🔌 API Endpoints

### 1. ดึงการแจ้งเตือนของผู้ใช้
```bash
GET /api/notifications?limit=20
Authorization: Bearer {token}

Response:
{
  "data": [
    {
      "id": 1,
      "userId": 1,
      "type": "TICKET_ASSIGNED",
      "title": "มีงานถูกมอบหมายให้คุณ",
      "message": "Admin ได้มอบหมายงาน TKT001 ให้คุณ",
      "status": "UNREAD",
      "ticketId": 10,
      "actionUrl": "/tickets/10",
      "createdAt": "2025-12-21T10:30:00Z"
    }
  ],
  "unreadCount": 5,
  "total": 1
}
```

### 2. ดึงจำนวนการแจ้งเตือนที่ยังไม่ได้อ่าน
```bash
GET /api/notifications/unread-count
Authorization: Bearer {token}

Response:
{
  "unreadCount": 5
}
```

### 3. ทำเครื่องหมายว่าอ่านแล้ว
```bash
PATCH /api/notifications/:id/read
Authorization: Bearer {token}

Response:
{
  "message": "Notification marked as read",
  "data": { ... }
}
```

### 4. ทำเครื่องหมายว่าอ่านแล้วทั้งหมด
```bash
POST /api/notifications/mark-all-read
Authorization: Bearer {token}

Response:
{
  "message": "All notifications marked as read"
}
```

### 5. ลบการแจ้งเตือน
```bash
DELETE /api/notifications/:id
Authorization: Bearer {token}

Response:
{
  "message": "Notification deleted"
}
```

### 6. ลบการแจ้งเตือนทั้งหมด
```bash
DELETE /api/notifications
Authorization: Bearer {token}

Response:
{
  "message": "All notifications deleted"
}
```

## 👥 Role-Based Notifications

### ADMIN (ผู้ดูแลระบบ)
- ✅ รับแจ้งเตือนเมื่อมีงานใหม่ได้รับการสร้าง
- ✅ รับแจ้งเตือนเมื่อมีการเปลี่ยนแปลงสถานะงาน
- ✅ รับแจ้งเตือนเมื่อมีการอัปเดตงาน

### IT (IT Support)
- ✅ รับแจ้งเตือนเมื่อมีงานถูกมอบหมายให้
- ✅ รับแจ้งเตือนเมื่อมีการเพิ่มความเห็นในงาน
- ✅ รับแจ้งเตือนเมื่องานของตนถูกอัปเดต

### USER (ผู้ใช้ปกติ)
- ✅ รับแจ้งเตือนเมื่องานของตนเปลี่ยนสถานะ
- ✅ รับแจ้งเตือนเมื่อมีการเพิ่มความเห็นในงาน
- ✅ รับแจ้งเตือนเมื่องานเสร็จสิ้น

## 🚀 การใช้งาน

### ฟรอนเต็นด์
```tsx
// หน้า Notifications
import NotificationsPage from '@/app/notifications/page';

// Notification Bell Component
import NotificationBell from '@/components/NotificationBell';
```

### การรับแจ้งเตือนแบบอัตโนมัติ
```typescript
// NotificationService ใน Backend

// เมื่อมีงานใหม่สร้างขึ้น
await notificationService.notifyAdminTicketCreated(
  ticketId,
  ticketCode,
  userName,
);

// เมื่อมีงานถูกมอบหมาย
await notificationService.notifyTicketAssigned(
  ticketId,
  assignedUserId,
  assignedBy,
);

// เมื่อสถานะของงานเปลี่ยน
await notificationService.notifyTicketStatusChanged(
  ticketId,
  userId,
  newStatus,
);
```

## 📱 หน้าแสดงการแจ้งเตือน

### `/notifications` - หน้าแจ้งเตือนเต็มรูป
Features:
- ✅ แสดงรายการแจ้งเตือนทั้งหมด
- ✅ กรองตามสถานะ (All, Unread, Read)
- ✅ ทำเครื่องหมายว่าอ่านแล้ว
- ✅ ลบการแจ้งเตือน
- ✅ คลิกเพื่อไปยังงาน

### Notification Bell - Dropdown
Features:
- ✅ แสดงแจ้งเตือนล่าสุด 5 รายการ
- ✅ แสดงตัวเลขแจ้งเตือนที่ยังไม่ได้อ่าน
- ✅ ตรวจสอบอัตโนมัติทุก 30 วินาที
- ✅ ลิงก์ไปยังหน้าแจ้งเตือนเต็มรูป

## 📊 ตัวอย่างการใช้งาน

### สร้างแจ้งเตือนใหม่
```typescript
// ในการสร้างงานใหม่
const ticket = await ticketService.create(createTicketDto);

// แจ้งเตือน Admin
await notificationService.notifyAdminTicketCreated(
  ticket.id,
  ticket.ticketCode,
  user.name,
);
```

### โหลดการแจ้งเตือนของผู้ใช้
```typescript
const notifications = await notificationService.getUserNotifications(
  userId,
  20, // limit
);
```

## ✅ Checklist การตั้งค่า

- [ ] สร้าง Notification model ในฐานข้อมูล
- [ ] สร้าง NotificationService
- [ ] สร้าง NotificationController
- [ ] เพิ่ม NotificationModule ในแอพลิเคชัน
- [ ] สร้างหน้า Notifications ใน Frontend
- [ ] สร้าง NotificationBell component
- [ ] เพิ่มลิงก์ notifications ในหน้า Profile/Sidebar
- [ ] ทดสอบการสร้างและการแจ้งเตือน

## 🔐 ความปลอดภัย

- ✅ ทุกการร้องขอต้องมี JWT token
- ✅ ผู้ใช้สามารถเห็นเฉพาะการแจ้งเตือนของตนเท่านั้น
- ✅ ข้อมูลการแจ้งเตือนถูกลบเมื่อผู้ใช้ถูกลบ (CASCADE)

